db:
  user: postgres
  password: password
  dbname: myapp
  host: postgres
  port: 5432
logger:
  level: info
  console:
    enable: true
    encoding: json
  file:
    dirpath: logs
    enable: false
    maxage:
    maxsize:
