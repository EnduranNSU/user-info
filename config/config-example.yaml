db:
  user: postgres
  password: password
  dbname: myapp
  host: localhost
  port: 5432
logger:
  level: info
  console:
    enable: true
    encoding: json
  file:
    dirpath: logs
    enable: false
    maxage:
    maxsize:
http:
  addr: ":8080" 